#include<stdio.h>

int main(){
    // char *ptr="Shivesh";
    // printf("%s",ptr);

    char s[100];
    // scanf("%s",s);
    gets(s);
    puts(s);
    // printf("Your name is %s",s);
    return 0;
}